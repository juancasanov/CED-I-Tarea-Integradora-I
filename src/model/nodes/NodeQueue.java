package model.nodes;

import model.templates.Node;

public class NodeQueue<T extends Comparable<T>> extends Node<T> {
// The code `public NodeQueue(T t){ super(t); }` is a constructor for the `NodeQueue` class.
    public NodeQueue(T t){
        super(t);
    }
}